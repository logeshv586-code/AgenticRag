# Unified Engine behavior

The default route now renders the existing full OmniRAG Engine and layers the global natural-language RAG Autopilot on top. Users describe the desired outcome; architecture selection is automatic, and a build starts automatically when trusted knowledge is available. The `/advanced` URL remains a compatibility alias to the same unified experience.
